@page
@model IndexModel
@{
    ViewData["Title"] = "Sezóny";
}

@if (Model.StatusMessage is not null)
{
    <div class="infobox">
        <i class="fa-solid fa-circle-check fa-fw"></i> @Model.StatusMessage
    </div>
}

<form method="get" class="filter-form">
    <div class="filter-row">
        <div class="field">
            <label asp-for="Search">Název</label>
            <input asp-for="Search" type="search" placeholder="Hledat sezónu…" />
        </div>
        <div class="field">
            <label asp-for="IsActive">Stav</label>
            <select asp-for="IsActive">
                <option value="true">Aktivní</option>
                <option value="false">Neaktivní</option>
                <option value="">Vše</option>
            </select>
        </div>
        <div class="filter-actions">
            <button type="submit" class="button">
                <i class="fa-solid fa-magnifying-glass fa-fw"></i> Hledat
            </button>
            <a class="button secondary" asp-page="Index">
                <i class="fa-solid fa-filter-circle-xmark fa-fw"></i> Vymazat filtr
            </a>
        </div>
    </div>
</form>

<p>
    <a class="button" asp-page="Edit">
        <i class="fa-solid fa-plus fa-fw"></i> Přidat sezónu
    </a>
</p>

@if (Model.Seasons.Count == 0)
{
    <p class="text-muted">Žádné sezóny neodpovídají filtru.</p>
}
else
{
    <table class="grid">
        <thead>
            <tr>
                <th>Název</th>
                <th>Od</th>
                <th>Do</th>
                <th>Stav</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var season in Model.Seasons)
            {
                <tr class="@(!season.IsActive ? "inactive-row" : null)">
                    <td>@season.Name</td>
                    <td>@season.From.ToString("d")</td>
                    <td>@season.To.ToString("d")</td>
                    <td>@(season.IsActive ? "Aktivní" : "Neaktivní")</td>
                    <td class="buttons">
                        <a class="button secondary icon-btn"
                           asp-page="Edit"
                           asp-route-id="@season.Id"
                           title="Upravit">
                            <i class="fa-solid fa-pen fa-fw"></i>
                        </a>
                        <form method="post"
                              asp-page-handler="SetActive"
                              asp-route-id="@season.Id"
                              asp-route-isActive="@(!season.IsActive)"
                              asp-route-search="@Model.Search"
                              style="display:inline">
                            <button type="submit"
                                    class="button tertiary icon-btn"
                                    title="@(season.IsActive ? "Zneaktivnit" : "Aktivovat")"
                                    onclick="@(season.IsActive ? "return confirm('Opravdu chcete sezónu zneaktivnit?')" : null)">
                                <i class="fa-solid @(season.IsActive ? "fa-trash" : "fa-rotate-left") fa-fw"></i>
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
